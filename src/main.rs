#![allow(dead_code)]
#![allow(unused_variables)]
#![allow(non_camel_case_types)]

mod bit_vec;
mod rank_support;

use crate::bit_vec::*;
use rank_support::*;
use std::fmt::{Display, Formatter};
use std::vec::Vec;

fn main() {}
